# Copy to .env.local — never commit real values
NEXT_PUBLIC_CONVEX_URL=
RESEND_API_KEY=
RESEND_FROM=
# Convex-only (set via `bunx convex env set KEY value` per deployment, not here):
# RESEND_API_KEY, RESEND_FROM
# Netlify-only (set via `bunx netlify env:set`):
# NEXT_PUBLIC_CONVEX_URL
